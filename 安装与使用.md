# 笔记拾荒者 v0.3 · 安装与使用

写作时从 vault 旧笔记里召回 **相似 / 反向 / 案例 / 金句** 片段;基于人工标记的 ★ 锚点做 **AI 改写 query + 迭代检索**,让搜索方向越标越精准。

---

## 一、安装(PowerShell 一键)

打开 **PowerShell**,改一下 `$vault` 那行,然后整段贴进去回车:

```powershell
# 1. 改成你 Obsidian vault 的根目录(包含 .obsidian/ 那个目录)
$vault = "C:\Users\你的用户名\Documents\我的Vault"

# 2. 起这行不用动:从当前目录解压
$zip   = Join-Path $PWD "笔记拾荒者-0.3.0.zip"
$dest  = Join-Path $vault ".obsidian\plugins"
if (-not (Test-Path $vault)) { throw "找不到 vault: $vault" }
if (-not (Test-Path $zip))   { throw "找不到压缩包: $zip。PowerShell 先 cd 到 zip 所在目录" }
New-Item -ItemType Directory -Path $dest -Force | Out-Null
Expand-Archive -Path $zip -DestinationPath $dest -Force
Write-Host "OK 已安装到 $dest\biji-huangzhe" -ForegroundColor Green
Write-Host "下一步:打开 Obsidian -> 设置 -> 第三方插件 -> 关闭安全模式 -> 启用「笔记拾荒者」"
```

直接粘贴运行的脚本不受执行策略限制,如果遇到"无法加载文件"那是双击 .ps1 才会有的限制,本流程不受影响。

### 手动安装

解压得到 `biji-huangzhe/` 文件夹,放进 `<你的vault>\.obsidian\plugins\` 即可。

---

## 二、首次配置

启用插件后 → **设置 → 笔记拾荒者**:

| 字段 | 推荐 | 说明 |
|---|---|---|
| API Endpoint | `https://dashscope.aliyuncs.com/compatible-mode/v1` | 阿里云百炼(默认已填) |
| Model | `text-embedding-v4` | 默认已填 |
| Dimensions | `2048` | 默认已填 |
| **API Key** | 填你自己的 | 阿里云百炼控制台 → API-KEY 管理 |
| **Chat Model** | `qwen-plus` | **锚点搜索必须配 chat**(下面那一节"LLM 二段式判定") |

滚到最底点 **「开始索引」**,vault 全部切块算 embedding,100 篇笔记几分钟。

完成后:
- 任意笔记开始写,右侧自动出卡片
- 或者顶部搜索框输文字回车,语义搜索

> **API key 安全**:不想存进 data.json?设环境变量 `BIJI_API_KEY` + 设置里 API Key 留空,插件会优先用环境变量。chat 的 anthropic key 用 `BIJI_ANTHROPIC_API_KEY`。

---

## 三、★ 锚点搜索(v0.3 核心新功能)

**问题**:第一轮召回的卡片有几张方向对,但还想要更准、更窄、更聚焦的——传统搜索做不到这个。

**做法**:点 ★ 钉住你觉得对的卡片(锚点),AI 看到 query + 你的锚点 + 没钉的卡片(负反馈),**重写一个新的 query 文本**再去召回。

### 操作流程

1. 触发一次召回(写作 / 搜索框 / 右键拾)
2. 把鼠标移到卡片上,头部右侧出现 **★ 钉选按钮**(默认半透明,hover 浮现)
3. 点几个觉得对的卡片,★ 变成琥珀色
4. 顶部出现金色 refine bar:**「⭐ N 个锚点 · 以这些再找 →」**
5. 点 → 旧卡片整体模糊淡出,中央浮 **「AI · 召回中」+ 三点 bouncing**
6. 几秒后看到:
   - **AI · 理解** ← 一句话,AI 看懂了你在追问什么
   - **AI · 改写** ← « 新的 query 文本 »
   - **+ 概念词 / - 排除词** ← 锚点共有的核心 / 不要这些方向
   - 新卡片绽开

### 反复细化

新结果出来后,可以**继续钉 + 继续点 refine bar 上的「再细化 →」**。每轮 refine 都用上一轮的新 query 当起点,**标得越多 query 越精准**。

锚点最多用最近 6 个(防过拟合 + 防 prompt 膨胀)。LLM 检测到锚点互相矛盾会在红色警告区点出来,让你删某个锚点再试。

### 单次成本

每次 refine ≈ 1 次 chat API 调用 + 1 次 embedding,约 **¥0.01-0.05**(看 chatModel,qwen-plus 大概这个价位)。

---

## 四、命令(Ctrl+P)

10 个命令全部带 "笔记拾荒者:" 前缀:

| 命令 | 用途 |
|---|---|
| 打开拾荒面板 | 右侧面板 |
| 索引整个 vault | 重算所有 embedding |
| 手动拾一下 | 当前段落立即触发(不等停顿) |
| 切换专注模式 | 暂停/恢复召回 |
| 切换反打扰 | 冷却 / 7 天去重 / 拉黑 总开关 |
| 切换自动召回 | 写作停顿 2s 自动拾 vs 只走显式触发 |
| 重新索引当前笔记 | 单文件强制重算 |
| 复制最后召回的卡片 | 纯文本到剪贴板 |
| **以钉选的锚点再找一次** | 不点 refine bar 也可以从命令面板触发 |
| 查看索引状态 | vault 数 / 已索引 / chunk / embedding / 错误 |
| 清空所有索引 | 8 秒内二次确认,防误触 |

可以在 **设置 → 快捷键** 给常用命令绑快捷键。

---

## 五、底栏状态条

Obsidian 右下角会显示一个 🪶 小图标:

- `🪶 X 篇 · N` — 就绪,已索引 X 篇 / N 个 embedding chunk
- `🪶 未配置` — 黄字,没填 API Key
- `🪶 索引 3/120` — 蓝字,正在索引
- `🪶 失败 ×N` — 红字,最近 N 次索引/召回失败(控制台 Ctrl+Shift+I 看详情)

**点击图标**会弹出详细状态。

---

## 六、卡片交互

hover 卡片才显示按钮(默认极简):

- **★** 钉为锚点 → 加入 refine bar 的锚点集合
- **看原文 →** 跳到原笔记并定位到那一段
- **有用 / 没用** 反馈(影响排序权重 + 反打扰)
- **别再提醒** 加入隐藏列表
- **底部时间小字** "3 个月前你写的" 风格,基于 mtime

色条:**蓝=相似 / 橙红=反向 / 绿=案例 / 琥珀=金句**

---

## 七、数据和隐私

所有数据存在 `<vault>\.obsidian\plugins\biji-huangzhe\`:
- `data.json` — 设置 + 反打扰状态 + chunks 元数据
- `embeddings.bin` — 二进制的 embedding 向量(占大头)

**如果 vault 跟 git / iCloud / OneDrive 同步**,强烈建议忽略:

```
.obsidian/plugins/biji-huangzhe/data.json
.obsidian/plugins/biji-huangzhe/embeddings.bin
```

API key 不想明文存盘?用环境变量,见 README"隐私 / API key 安全"段。

完全离线?接 Ollama 本地 embedding,详见源码 README。

---

## 八、卸载

```powershell
$vault = "C:\Users\你的用户名\Documents\我的Vault"
Remove-Item -Recurse -Force "$vault\.obsidian\plugins\biji-huangzhe"
```

---

**版本** 0.3.0 · **作者** 刘洋
